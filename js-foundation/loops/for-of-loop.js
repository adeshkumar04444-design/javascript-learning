// for...of loop: Iterates over the iterable objects (like arrays, strings, maps, sets, etc.)

const colors = ["red", "green", "blue"];
for (let color of colors) {
    console.log(color);
}

/*
Output:
red
green
blue
*/