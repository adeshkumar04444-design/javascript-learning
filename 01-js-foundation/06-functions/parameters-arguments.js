// Parameters vs Arguments

function add(a, b) {  // 'a' and 'b' are parameters.
    return a + b;
}
console.log(add(10, 20));  // 10 and 20 are arguments.


// Parameters receive values.
// Arguments are acual values passed during function call.
