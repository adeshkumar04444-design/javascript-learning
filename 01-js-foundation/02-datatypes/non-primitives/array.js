// Array: A special type of object used to store ordered collections of data. Arrays can hold elements of any data type, including other arrays and objects. They are zero-indexed, meaning the first element is accessed with index 0.

let colors = ["red", "green", "blue"];
console.log(typeof colors); // Output: "object" (Arrays are objects in JS)
console.log(colors[0]); // Output: "red"
console.log(colors.length); // Output: 3    